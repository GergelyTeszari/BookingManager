# Booking Manager

A small C++20 booking data structure supporting:

- efficient booking insertion
- selection by departure time
- selection by adjacent airport pair

## Build and test

On Windows with Visual Studio C++ Build Tools installed:

```powershell
.\build.ps1
```
The script builds and runs the test executable.